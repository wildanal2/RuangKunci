<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.keyroom.com.keyroom">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".User.MainActivity" />
        <activity android:name=".Admin.MainAdminActivity" />
        <activity android:name=".Admin.NewMahasiswa" />
        <activity android:name=".Admin.UserManager" />
        <activity android:name=".Admin.MeminjamKelas" />
        <activity android:name=".Admin.KelasManager" />
        <activity android:name=".Admin.AddKelas" />
        <activity android:name=".Admin.DetailRuangManagerFragmentAdmin" />
        <activity android:name=".User.GantiPassword"></activity>
    </application>

</manifest>